{
  "version": "v1",
  "hooks": [
    {
      "name": "Kiro Passport Session Idle",
      "trigger": "Stop",
      "description": "Pushes the agent's final message to the AI Passport when the Kiro agent completes a turn. The bridge sanitizes the hook's stdin payload and calls the relay notify API.",
      "action": {
        "type": "command",
        "command": "python3 tools/kiro_passport_bridge.py hook --kind idle",
        "timeout": 10
      },
      "enabled": true
    }
  ]
}