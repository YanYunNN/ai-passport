build/
# PC simulator build output
simulator/build/
simulator/firmware/
# ESP-IDF build tree (named esp-idf) and the SDK clone nested inside it.
esp-idf/
# Stray CMake/bootloader residue left at the repo root.
bootloader/
bootloader-prefix/
gdbinit/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock
.vscode/
cloudflare/node_modules/
cloudflare/worker-configuration.d.ts
cloudflare/node_modules
.kiro/passport_config.json
.kiro_passport.json